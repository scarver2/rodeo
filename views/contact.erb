<div class="megacontainer prose">
  <h1>Contact Us</h1>

  <p>We&rsquo;re happy to answer your questions or provide a quote. Upload your artwork or photos to help us serve you better.</p>

<div class="grid grid-cols-1 sm:grid-cols-2">
  <div>
      <form action="/contact/new" method="POST">
        <% if @form.errors&.any? %>
          <div role="alert" class="errors">
            <p class="font-semibold">Please fix the following:</p>
            <ul class="mt-2 list-disc space-y-1 pl-5 text-sm">
              <% @form.errors.each do |field, msg| %>
                <li><strong class="capitalize"><%= field %></strong>: <%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="hidden" aria-hidden="true" style="display: none;">
          <label for="<%= ContactForm::HONEYPOT_FIELD %>" class="block text-sm font-medium text-stone-800"><%= ContactForm::HONEYPOT_FIELD %></label>
          <input
            type="text"
            id="<%= ContactForm::HONEYPOT_FIELD %>"
            name="<%= ContactForm::HONEYPOT_FIELD %>"
            value=""
            autocomplete="off"
            tabindex="-1"
            class="block w-full rounded-xl border border-stone-300 bg-white px-4 py-3 text-stone-900 shadow-sm outline-none placeholder:text-stone-400 focus:border-stone-900 focus:ring-2 focus:ring-stone-900/20"
            placeholder="Your <%= ContactForm::HONEYPOT_FIELD %>"
          />
        </div>

        <div class="space-y-2">
          <label for="name" class="block text-sm font-medium text-stone-800">Name</label>
          <input
            type="text"
            id="name"
            name="name"
            required="required"
            value="<%= @form.name %>"
            autocomplete="name"
            class="block w-full rounded-xl border border-stone-300 bg-white px-4 py-3 text-stone-900 shadow-sm outline-none placeholder:text-stone-400 focus:border-stone-900 focus:ring-2 focus:ring-stone-900/20"
            placeholder="Your name"
          />
        </div>

        <div class="space-y-2">
          <label for="email" class="block text-sm font-medium text-stone-800">Email</label>
          <input
            type="email"
        id="email"
        name="email"
                    required="required"

        value="<%= @form.email %>"
        autocomplete="email"
        class="block w-full rounded-xl border border-stone-300 bg-white px-4 py-3 text-stone-900 shadow-sm outline-none placeholder:text-stone-400 focus:border-stone-900 focus:ring-2 focus:ring-stone-900/20"
        placeholder="you@example.com"
        />
      </div>

      <div class="space-y-2">
          <label for="phone" class="block text-sm font-medium text-stone-800">Phone</label>
          <input
          type="tel"
          id="phone"
          name="phone"
                      required="required"

          value="<%= @form.phone %>"
          autocomplete="phone"
          class="block w-full rounded-xl border border-stone-300 bg-white px-4 py-3 text-stone-900 shadow-sm outline-none placeholder:text-stone-400 focus:border-stone-900 focus:ring-2 focus:ring-stone-900/20"
          placeholder="(123) 456-7890"
          />
      </div>

      <div class="space-y-2">
          <label for="message" class="block text-sm font-medium text-stone-800">Message</label>
          <textarea
          id="message"
          name="message"
          rows="6"
          class="block w-full resize-y rounded-xl border border-stone-300 bg-white px-4 py-3 text-stone-900 shadow-sm outline-none placeholder:text-stone-400 focus:border-stone-900 focus:ring-2 focus:ring-stone-900/20"
          placeholder="How can we help?"
          ><%= @form.message %></textarea>
      </div>

      <div class="space-y-2">
          <label for="attachment" class="block text-sm font-medium text-stone-800">Attachment</label>
          <input
          type="file"
          id="attachment"
          name="attachment"
          class="block w-full rounded-xl border border-stone-300 bg-white px-4 py-3 text-stone-900 shadow-sm outline-none placeholder:text-stone-400 focus:border-stone-900 focus:ring-2 focus:ring-stone-900/20"
          />
          <p class="hint">Attach artwork or photo. Adobe Illustrator, EPS, GIF, JPEG, PDF, or PNG format supported.</p>
      </div>

      <input type="hidden" name="started_at" value="<%= Time.now %>" />

      <div class="pt-2">
          <button
          type="submit"
          style="background-color: #dc823b; color: #fff; padding: 1em 2em; border-radius: 4px; border: none; cursor: pointer;"
          >
          Send
          </button>
          <p class="text-xs text-stone-500">We typically reply within one business hour.</p>
      </div>
      </form>
    </div>
    <div>
      <!-- TODO: add better image of map and link to map -->
      <img src="/images/map-20250711.png" alt="map" />
    </div>
  </div>
</div>
 